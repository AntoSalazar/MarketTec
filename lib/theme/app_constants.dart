import 'package:flutter/material.dart';

/// Colores globales reutilizables en la app
const Color kLightBackgroundColor = Color(0xFFFFFBF0);

// Aquí puedes agregar más constantes globales, por ejemplo:
// const double kDefaultPadding = 16.0;
// const Duration kAnimationDuration = Duration(milliseconds: 300);
